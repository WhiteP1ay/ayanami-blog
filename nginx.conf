server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — 所有 /blog/* 路由指向 index.html
    location /blog {
        try_files $uri $uri/ /blog/index.html;
    }

    # Cache static assets
    location /blog/assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
